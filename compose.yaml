services:
  interviewlab:
    build:
      context: .
    image: interviewlab:local
    init: true
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    environment:
      HOSTNAME: 0.0.0.0
      PORT: 3000
      SITE_URL: "${SITE_URL:-http://localhost:3000}"
